<div class="flex flex-row justify-between bg-gray-100">
    <div class="flex justify-start flex-row list-none bg-gray-100 p-2 gap-2">
        <li class="flex justify-center items-center m-2">
            <strong>Condomínio Simples</strong>
        </li>
    </div>

    <div class="flex justify-end items-center flex-row list-none bg-gray-100 p-2 gap-2">
        <% if authenticated? %>
          <li>
              <%= Current.user.email_address %>
          </li>
          <li>
            <%= button_to "Sair", session_path, method: :delete, class: "bg-red-500 text-white px-4 py-2 rounded" %>
          </li>
        <% else %>
          <li>
            <%= link_to "Entrar", new_session_path, class: "bg-blue-500 text-white px-4 py-2 rounded" %>
          </li>
        <% end %>
    </div>
</div>
